<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shashank.admin.ncrb_new_admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Add_Activity_police"></activity>
        <activity android:name=".FirWithFirNo" />
        <activity android:name=".AvailableCops" />
        <activity android:name=".AssignmentFir" />
        <activity android:name=".Add_Beat" />
        <activity android:name=".FirAdmin" />
        <activity android:name=".MainActivityVerification" />
        <activity android:name=".AdminPage" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>